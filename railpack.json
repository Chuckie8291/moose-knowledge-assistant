{
  "provider": "python",
  "sourceDir": ".",
  "build": {
    "command": "pip install -r backend/requirements.txt"
  },
  "start": {
    "command": "python -m uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}"
  }
}
